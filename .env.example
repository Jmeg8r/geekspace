# Geekspace environment configuration.
# Copy to .env.local (gitignored) and fill in. Everything here is optional unless
# noted: the app runs with sane defaults and degrades gracefully when a given
# integration isn't configured.

# --- Convex (local backend) -------------------------------------------------
# Written automatically by `npx convex dev`; the packaged app manages its own.
CONVEX_DEPLOYMENT=anonymous:anonymous-agent
VITE_CONVEX_URL=http://127.0.0.1:3210
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211

# --- Local agent (Ollama) lane ----------------------------------------------
# Model that drives the local ARCHITECT lane (defaults to a qwen3-coder build).
# GEEKSPACE_LOCAL_MODEL=qwen3-coder
# GEEKSPACE_LOCAL_NUM_CTX=32768

# --- Enterprise Search: ASTGL knowledge server (optional) -------------------
# Absolute path to the built mcp-astgl-knowledge stdio server (…/dist/index.js).
# Leave unset and the Knowledge page / ⌘K knowledge results stay offline.
# GEEKSPACE_KNOWLEDGE_SERVER=/absolute/path/to/mcp-astgl-knowledge/dist/index.js
# Optional higher daily quota for that server (anonymous = 50 queries/day).
# ASTGL_API_KEY=
# Node binary used to spawn the knowledge server (native-module ABI). Auto-
# detected from PATH / Homebrew; override only if detection fails.
# GEEKSPACE_NODE=/opt/homebrew/bin/node
# Windows example: GEEKSPACE_NODE=C:\Program Files\nodejs\node.exe

# --- Reader: aib-reader RSS substrate (optional) ----------------------------
# The `aib-reader-mcp` console script (a local Python venv entry point) powering
# the Reader page. Auto-detected from PATH / ~/.local/bin; set this only if it
# isn't there — e.g. point it at the project venv so it tracks the editable
# source the aib-pipeline cron uses. Reader only goes offline if the executable
# can't be resolved at all (env override, then PATH, then ~/.local/bin).
# GEEKSPACE_READER_BIN=/absolute/path/to/aib-reader/.venv/bin/aib-reader-mcp
# Windows example: GEEKSPACE_READER_BIN=C:\path\to\aib-reader\.venv\Scripts\aib-reader-mcp.exe
# aib-reader resolves config/feeds.yaml relative to CWD; the Electron main process
# CWD isn't the aib-reader dir, so point add/remove at the canonical file.
# AIB_READER_FEEDS_CONFIG=/absolute/path/to/aib-reader/config/feeds.yaml
#
# PACKAGED app (.app): it can't read this .env.local (outside the bundle). Put the
# same two paths in ~/Library/Application Support/Geekspace/reader.json instead
# (Windows: %APPDATA%\Geekspace\reader.json):
#   { "readerBin": "…/aib-reader-mcp", "feedsConfig": "…/config/feeds.yaml" }
# so add/remove/poll in the double-clicked app write the SAME canonical feeds.yaml.

# --- Claude agent lane via ClaudeClaw (optional) ----------------------------
# Bearer token for the ClaudeClaw daemon that backs the hosted ARCHITECT lane.
# CLAUDECLAW_TOKEN=
